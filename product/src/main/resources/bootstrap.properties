# spring.app.name here is different from that in the application.properties file
#   here it is to set default name to search config, and in application.properties it's to assign the name of the service
spring.application.name=ecommerce-product-service.yml
spring.cloud.nacos.config.server-addr=127.0.0.1:8848
spring.cloud.nacos.config.namespace=dev
spring.cloud.nacos.config.file-extension=yaml
